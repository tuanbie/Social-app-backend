# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Conversation {
  """Example field (placeholder)"""
  exampleField: Int!
}

input CreateConversationInput {
  """Example field (placeholder)"""
  exampleField: Int!
}

input CreatePostInput {
  """
  SurrealDB record id của user, ví dụ "user:abc123"
  """
  authorId: ID!
  content: String
  files: [String!]
  image: String
  status: PostStatus = published
}

input CreateUserInput {
  avatar: String
  bio: String
  email: String!
  full_name: String!
  password: String!
  status: UserStatus = ACTIVE
  username: String!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Mutation {
  createConversation(createConversationInput: CreateConversationInput!): Conversation!
  createPost(createPostInput: CreatePostInput!): Post!
  createUser(createUserInput: CreateUserInput!): User!
  removeConversation(id: Int!): Conversation!
  removePost(id: ID!): Post!
  removeUser(id: ID!): User!
  updateConversation(updateConversationInput: UpdateConversationInput!): Conversation!
  updatePost(updatePostInput: UpdatePostInput!): Post!
  updateUser(updateUserInput: UpdateUserInput!): User!
}

type Post {
  author: ID!
  content: String
  created_at: DateTime!
  files: [String!]!
  id: ID!
  image: String
  status: PostStatus!
}

input PostQueryInput {
  authorId: ID
  keyword: String
  limit: Int = 10
  page: Int = 1
  status: PostStatus
}

enum PostStatus {
  pending
  published
}

type Query {
  conversation(id: Int!): Conversation!
  post(id: ID!): Post!
  posts(query: PostQueryInput): [Post!]!
  user(id: ID!): User!
}

input UpdateConversationInput {
  """Example field (placeholder)"""
  exampleField: Int
  id: Int!
}

input UpdatePostInput {
  """
  SurrealDB record id của user, ví dụ "user:abc123"
  """
  authorId: ID
  content: String
  files: [String!]

  """
  SurrealDB record id, ví dụ "post:abc123"
  """
  id: ID!
  image: String
  status: PostStatus = published
}

input UpdateUserInput {
  avatar: String
  bio: String
  email: String
  full_name: String
  id: ID!
  password: String
  status: UserStatus = ACTIVE
  username: String
}

type User {
  avatar: String
  bio: String
  created_at: DateTime!
  email: String
  full_name: String
  id: ID!
  status: UserStatus!
  username: String
}

enum UserStatus {
  ACTIVE
  BLOCK
}